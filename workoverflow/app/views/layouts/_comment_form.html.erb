<%= form_for @comment, url: admins_project_comments_path(@project) do |f| %>
  <% if @comment.errors.any? %>
  <div id="errors_explanation">
    <h2><%= pluralize(@comment.errors.count, "error") %>
      prohibited the user from being saved:</h2>
      <ul>
        <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
      </ul>
  </div>
  <% end %>

  <div>
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>

  <div><br>
    <%= f.submit "Comment!" %>
  </div>
<% end %>
